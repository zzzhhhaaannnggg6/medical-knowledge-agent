# 5 月 10 日黑客松总控台

## 一句话原则

先交付一个可运行、可部署、可提交的 MVP，再追求加分项。比赛中所有群消息和链接都要核验后落成行动项。

## 总控台长期约束

这个项目里的黑客松相关对话全部视为同一场竞赛的分支对话。每个分支可以分别处理群消息、赛题、代码、部署、文档，但最终都要同步回这套总控台文件。

## 立即入口

```bash
cd "/Users/li/Documents/New project 4"
./hackctl.sh status
```

如果要一次性打开主要资源：

```bash
./hackctl.sh open
```

如果是新对话或中断恢复：

```bash
./hackctl.sh sync
```

如果你忘了怎么和 Agent 配合：

```bash
./hackctl.sh agent
```

如果要查看当前 5 小时阶段执行板：

```bash
./hackctl.sh phase
```

如果要分派多个 Agent 或多台电脑：

```bash
./hackctl.sh dispatch
```

## 当前已确认

- 时间：2026 年 5 月 10 日 9:00 A.M.
- 地点：北 2-112
- 形式：个人赛
- 时长：5 小时
- 赛制重点：AI 原生开发，0 路演，只看作品
- 目标：独立完成一个可运行、可体验、能说明问题的 AI 应用
- 允许工具：Codex、Claude、Cursor、Trae 等
- 已下载教材：`/Users/li/Desktop/黑客松/textbooks`
- 提交入口：飞书收集表
- 当前正式阶段表：`control_center/18_PHASE_TASK_BREAKDOWN.md`

## 明天开赛前 10 分钟

- 打开微信并确认 `浙大AI全栈极速黑客松！` 群可读。
- 打开 ModelScope 并确认能登录。
- 运行 `./hackctl.sh status`。
- 运行 `gh auth status`，如果未登录就执行 `gh auth login`。
- 打开赛题文档后，先让 Codex 做题目拆解，不要马上开写。

## 比赛中我的总控台职责

- 读题后拆成 `必做 MVP / 提分项 / 放弃项`。
- 把赛题文档、群通知、提交表单这三类来源分开核验，冲突时以最新官方通知为准。
- 每出现一个群链接，打开并分析，不凭气泡文本判断。
- 每 10-15 分钟检查一次群消息，记录到 `03_WECHAT_AND_LINK_AUDIT.md`。
- 关键决策写入 `08_DECISION_LOG.md`，任务分支写入 `09_TASK_TREE.md`，中断交接写入 `10_HANDOFF.md`。
- 帮你把每个功能压成可实现的最小闭环。
- 盯住最终提交：Public GitHub 仓库、公网部署链接、可选技术报告。

## 开赛后第一句话

拿到赛题后直接对我说：

> 赛题来了，开始总控。

我会立刻按 `读题 -> 评分点 -> MVP -> 技术栈 -> 任务队列 -> 30 分钟交付目标` 的顺序推进。

## Agent 小白使用协议

你不需要先懂 Agent 或全栈开发。比赛中只要把真实材料贴给我：

- `赛题来了，开始总控`
- `这是群消息，帮我判断`
- `这个链接是不是重要`
- `现在应该做什么`
- `把方案压缩成 MVP`
- `进入最终提交模式`

固定协议见：`control_center/16_AGENT_BEGINNER_PROTOCOL.md`。

## 当前人工阻塞

微信客户端刚才触发账号安全重新登录并出现黑屏。你需要先手动恢复微信登录。恢复后直接告诉我：“微信好了，继续看群”。
